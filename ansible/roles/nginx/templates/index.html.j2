<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CERBERUS - Guardian of Infrastructure</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a0a1a 50%, #0d0d0d 100%);
            color: #fff;
            overflow-x: hidden;
        }

        .container {
            text-align: center;
            padding: 40px;
            max-width: 900px;
        }

        .logo {
            font-size: 4.5rem;
            font-weight: bold;
            margin-bottom: 5px;
            background: linear-gradient(90deg, #ff6b00, #9b30ff, #ff6b00);
            background-size: 200% auto;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: gradient 3s ease infinite;
            text-shadow: 0 0 30px rgba(155, 48, 255, 0.5);
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .author {
            font-size: 1rem;
            color: #ff6b00;
            margin-bottom: 10px;
            font-family: 'Courier New', monospace;
            opacity: 0.9;
        }

        .subtitle {
            font-size: 1.1rem;
            color: #9b30ff;
            margin-bottom: 30px;
            text-transform: uppercase;
            letter-spacing: 4px;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: rgba(255, 107, 0, 0.1);
            border: 2px solid #ff6b00;
            padding: 15px 30px;
            border-radius: 50px;
            margin-bottom: 40px;
            box-shadow: 0 0 25px rgba(255, 107, 0, 0.4);
        }

        .status-badge .dot {
            width: 12px;
            height: 12px;
            background: #00ff88;
            border-radius: 50%;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { box-shadow: 0 0 0 0 rgba(0, 255, 136, 0.7); }
            50% { box-shadow: 0 0 0 12px rgba(0, 255, 136, 0); }
        }

        .status-badge span {
            color: #ff6b00;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .info-box {
            background: rgba(155, 48, 255, 0.1);
            border: 1px solid rgba(155, 48, 255, 0.4);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            backdrop-filter: blur(10px);
        }

        .info-box h2 {
            color: #ff6b00;
            margin-bottom: 15px;
            font-size: 1.4rem;
        }

        .info-box p {
            color: #ccc;
            margin-bottom: 10px;
        }

        .node-name {
            color: #9b30ff;
            font-weight: bold;
            font-size: 1.2rem;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin-top: 20px;
        }

        .card {
            background: rgba(155, 48, 255, 0.08);
            border: 1px solid rgba(155, 48, 255, 0.3);
            border-radius: 12px;
            padding: 25px;
            transition: all 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            border-color: #ff6b00;
            box-shadow: 0 10px 30px rgba(255, 107, 0, 0.3);
        }

        .card h3 {
            color: #ff6b00;
            margin-bottom: 10px;
            font-size: 0.95rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .card p {
            color: #9b30ff;
            font-size: 1.1rem;
            font-family: 'Courier New', monospace;
        }

        .card a {
            color: #9b30ff;
            text-decoration: none;
            transition: color 0.3s;
        }

        .card a:hover {
            color: #ff6b00;
        }

        .footer {
            margin-top: 40px;
            color: #666;
            font-size: 0.9rem;
        }

        .footer span {
            color: #ff6b00;
        }

        /* Fire grid background */
        .fire-grid {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(255, 107, 0, 0.02) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 107, 0, 0.02) 1px, transparent 1px);
            background-size: 50px 50px;
            pointer-events: none;
            z-index: -1;
        }

        /* Glowing orbs - Hellfire theme */
        .orb {
            position: fixed;
            border-radius: 50%;
            filter: blur(100px);
            pointer-events: none;
            z-index: -1;
        }

        .orb-1 {
            width: 500px;
            height: 500px;
            background: rgba(155, 48, 255, 0.25);
            top: -150px;
            right: -150px;
        }

        .orb-2 {
            width: 400px;
            height: 400px;
            background: rgba(255, 107, 0, 0.2);
            bottom: -100px;
            left: -100px;
        }

        .orb-3 {
            width: 300px;
            height: 300px;
            background: rgba(139, 0, 0, 0.15);
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        /* Cerberus icon */
        .cerberus-icon {
            font-size: 3rem;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="fire-grid"></div>
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>

    <div class="container">
        <div class="cerberus-icon">🔥</div>
        <h1 class="logo">CERBERUS</h1>
        <p class="author">By Mishka-sys</p>
        <p class="subtitle">Active Directory • Zabbix • Ansible</p>

        <div class="status-badge">
            <div class="dot"></div>
            <span>Service Opérationnel</span>
        </div>

        <div class="info-box">
            <h2>⚡ Cluster Haute Disponibilité</h2>
            <p>Ce serveur fait partie d'un cluster actif/passif</p>
            <p>Nœud actuel : <span class="node-name">{{ ansible_hostname }}</span></p>
        </div>

        <div class="grid">
            <div class="card">
                <h3>🌐 VIP (IP Flottante)</h3>
                <p><a href="http://192.168.56.100">192.168.56.100</a></p>
            </div>
            <div class="card">
                <h3>🖥️ Serveur Actuel</h3>
                <p>{{ ansible_hostname }}.cerberus.local</p>
            </div>
            <div class="card">
                <h3>🔧 Node01</h3>
                <p><a href="http://192.168.56.21">192.168.56.21</a></p>
            </div>
            <div class="card">
                <h3>🔧 Node02</h3>
                <p><a href="http://192.168.56.22">192.168.56.22</a></p>
            </div>
            <div class="card">
                <h3>📊 Zabbix</h3>
                <p><a href="http://192.168.56.10">192.168.56.10</a></p>
            </div>
            <div class="card">
                <h3>🏰 Active Directory</h3>
                <p>192.168.56.30 (RDP)</p>
            </div>
            <div class="card">
                <h3>⚙️ Services</h3>
                <p>Nginx + Samba</p>
            </div>
            <div class="card">
                <h3>🔗 Cluster</h3>
                <p>Pacemaker + Corosync</p>
            </div>
        </div>

        <div class="footer">
            Déployé automatiquement avec <span>Vagrant</span> & <span>Ansible</span> | Guardian of the Underworld
        </div>
    </div>
</body>
</html>
